import { describe, it, expect } from "@jest/globals";
import parseComponentTree from "./index";

describe("parseComponentTree", () => {
  it("should .", () => {
    expect(parseComponentTree).not.toBeNull()
  });
});
